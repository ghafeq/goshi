{
  "name": "@epds/consumer-tokens",
  "version": "0.1.0",
  "description": "Consumer (React Native) design tokens for EPDS — the con.* namespace, aliased from shared foundations plus con typography.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./css/con-light.css": "./build/css/con-light.css",
    "./css/con-dark.css": "./build/css/con-dark.css"
  },
  "files": [
    "dist",
    "build/css",
    "src"
  ],
  "scripts": {
    "tokens:build": "node scripts/build-tokens.mjs",
    "build": "pnpm tokens:build && tsc -p tsconfig.json",
    "typecheck": "pnpm tokens:build && tsc -p tsconfig.json --noEmit",
    "lint": "eslint src scripts --ext .ts,.mjs",
    "test": "vitest run",
    "clean": "rm -rf build dist src/generated"
  },
  "dependencies": {
    "@epds/consumer-typography": "workspace:*",
    "@epds/foundations-tokens": "workspace:*",
    "@epds/style-dictionary-config": "workspace:*"
  },
  "devDependencies": {
    "style-dictionary": "^4.1.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
