# Reserved — Consumer components

Not built yet. This stage covers foundations only (tokens, typography,
icons). Individual components (e.g. `Button`, `TextField`, `Card`) will each
become their own workspace package here — `packages/consumer/components/<name>`
— once the foundation is validated and Figma access is available for
component-level specs.
